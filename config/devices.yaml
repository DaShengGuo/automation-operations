# ============================================================
# 设备级配置 — devices.yaml
# 按 serial 覆盖默认行为（可选）
# ============================================================

overrides:
  # 示例: 禁用某台设备 / 单独调参
  # e98bee5a:
  #   disabled: false        # true = 该设备不参与自动化
  #   max_workers: 1

  # 真实设备会自动识别(品牌/型号/分辨率/DPI),
  # 无需在此登记即可使用 — 此文件只放「例外」。
