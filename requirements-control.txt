# 多设备游戏自动化中控系统 — Python 依赖
# 安装: python -m pip install -r requirements-control.txt
# 需要 Python 3.11+

# Android 自动化
uiautomator2>=3.2.0,<4.0.0

# 图像识别
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.26.0

# OCR(模型内置 wheel, 离线可用; 页面文字识别主力)
rapidocr-onnxruntime>=1.3.0

# 配置与数据处理
PyYAML>=6.0
python-dotenv>=1.0.0
pandas>=2.0.0
openpyxl>=3.1.0

# Web API
fastapi>=0.110.0
uvicorn>=0.29.0

# 账号 HTTP 导入
requests>=2.31.0

# 测试
pytest>=8.0.0

# 可选: OCR 页面识别 (安装体积较大, 默认关闭)
# paddleocr>=2.8.0,<3.0.0
# paddlepaddle>=2.6.0
